import * as React from "react";

interface HomeProps {
  sections: string[];
}

const Home: React.FC = () => {
  return (
    <div>
      <p>Why is everything a div? :/</p>
    </div>
  );
};

export default Home;
